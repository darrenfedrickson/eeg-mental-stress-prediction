services:
  backend:
    build: ./backend
    container_name: eeg_backend
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./frontend
    container_name: eeg_frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: always
